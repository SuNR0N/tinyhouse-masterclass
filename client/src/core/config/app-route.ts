export enum AppRoute {
    HOME = '/',
    HOST = '/host',
    LISTING = '/listing/:id',
    LISTINGS = '/listings/:location?',
    LOGIN = '/login',
    STRIPE = '/stripe',
    USER = '/user/:id',
}
